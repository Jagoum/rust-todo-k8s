apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: default
  labels:
    app: rust-todo-api
spec:
  selector:
    app: rust-todo-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
